import number_theory.dioph
import algebra.group
import tactic

open_locale classical

open set

#check is_poly



-- namespace silverman-tate

/- def is_limit (a : ℕ → ℝ) (α : ℝ) := 
  ∀ ε : ℝ, 0 < ε → ∃ N : ℕ, ∀ n : ℕ, N ≤ n → |a n - α| < ε -/


-- end silverman-tate
